package com.nr.synergysphere.common.enums;

public enum Role {
    ROLE_ADMIN,
    ROLE_USER
}

//to define a fixed set of constraints
//Centralizes all roles in one place
//fixed set of values, can use only these not anything else
//no typos
//cleaner and readable
//compile time safety : errors caught before running


//admin controls everything
//user normal user who'll be assigned a role during project